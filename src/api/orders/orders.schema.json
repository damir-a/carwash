{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "http://example.com/example.json",
  "type": "object",
  "title": "The orders",
  "description": "The orders schema.",
  "default": {},
  "examples": [
    {
      "client_id": 1,
      "order_id": 1,
      "service_id": 1,
      "user_id": 1,
      "car_id": 1
    }
  ],
  "required": ["client_id", "order_id", "service_id", "user_id", "car_id"],
  "properties": {
    "client_id": {
      "$id": "#/properties/client_id",
      "type": "integer",
      "title": "The client_id",
      "description": "Client_id in DB.",
      "default": 0,
      "examples": [1]
    },
    "order_id": {
      "$id": "#/properties/order_id",
      "type": "integer",
      "title": "Order_id",
      "description": "Order_id in DB.",
      "default": 0,
      "examples": [1]
    },
    "service_id": {
      "$id": "#/properties/service_id",
      "type": "integer",
      "title": "Service_id",
      "description": "Service_id in DB.",
      "default": 0,
      "examples": [1]
    },
    "user_id": {
      "$id": "#/properties/user_id",
      "type": "integer",
      "title": "User_id",
      "description": "User_id in DB.",
      "default": 0,
      "examples": [1]
    },
    "car_id": {
      "$id": "#/properties/car_id",
      "type": "integer",
      "title": "Car_id",
      "description": "Car_id in DB.",
      "default": 0,
      "examples": [1]
    },
    "id": {
      "$id": "#/properties/id",
      "type": "integer",
      "title": "id",
      "description": "id in DB.",
      "default": 0,
      "examples": [1]
    },
    "deleted_at": {
      "$id": "#/properties/deleted_at",
      "type": "string",
      "title": "Time of order deletion",
      "description": "Time of order deletion."
    },
    "updated_at": {
      "$id": "#/properties/updated_at",
      "type": "string",
      "title": "Time of order updation",
      "description": "Time of order updation."
    }
  },
  "additionalProperties": false
}
