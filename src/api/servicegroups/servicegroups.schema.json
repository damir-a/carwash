{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "http://example.com/example.json",
  "type": "object",
  "title": "Servicegroups schema",
  "description": "The Servicegroup schema.",
  "default": {},
  "examples": [
    {
      "title": "Эконом",
      "description": "Нисса микра, ока и другие"
    }
  ],
  "required": ["title"],
  "properties": {
    "title": {
      "$id": "#/properties/title",
      "type": "string",
      "title": "The title schema",
      "description": "Title for servicegroup.",
      "default": "",
      "examples": ["Эконом"]
    },
    "description": {
      "$id": "#/properties/description",
      "type": "string",
      "title": "The description schema",
      "description": "Description for servicegroup.",
      "default": "",
      "examples": ["Нисса микра, ока и другие"]
    },
    "uuid": {
      "$id": "#/properties/uuid",
      "type": "string",
      "title": "unique id",
      "description": "Unique id for servicegroup.",
      "default": "",
      "examples": ["514db767-bd17-4b0a-bf90-299a96ba29b8"]
    },
    "id": {
      "$id": "#/properties/id",
      "type": "integer",
      "title": "id for servicegroup",
      "description": "id for servicegroup.",
      "default": 0,
      "examples": [1]
    },
    "deleted_at": {
      "$id": "#/properties/id",
      "type": "string",
      "title": "Time of the servicegroup deletion",
      "description": "Time when servicegroup deleted."
    }
  },
  "additionalProperties": false
}
